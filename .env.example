# Copy to .env.local. Never commit actual credentials.
NEXT_PUBLIC_SUPABASE_URL=https://vtkykdzsaerjxbzzzglk.supabase.co
SUPABASE_PUBLISHABLE_KEY=
# Keep demo until the migrations and seed have been applied and tested.
NEXT_PUBLIC_DATA_MODE=demo
# Exact canonical origin used for authentication redirects.
APP_ORIGIN=http://localhost:3000
# Configure the matching secret in Supabase Auth > Attack Protection.
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
SUPABASE_CAPTCHA_ENABLED=false
